# Web framework
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Database (async)
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20          # default driver for zero-config quickstart
aiomysql>=0.2            # use this for MySQL (see .env.example)
alembic>=1.13            # production schema migrations

# Settings & validation
pydantic>=2.6
pydantic-settings>=2.2
email-validator>=2.1

# Auth
python-jose[cryptography]>=3.3
bcrypt>=4.0  # 直接使用 bcrypt（不依赖 passlib，跨 Python 3.12/3.13 均有预编译 wheel）

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
